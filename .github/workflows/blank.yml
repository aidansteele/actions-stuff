name: CI
on:
  push:
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - run: echo "hello world" | parallel -d " " -k bash bin.sh {}
        env:
          EVENT: ${{ toJson(github) }}

      - run: env | sort
        env:
          ACTOR_TYPE: ${{ github.event.sender.type }}
